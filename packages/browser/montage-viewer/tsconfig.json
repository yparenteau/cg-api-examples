{
    "extends": "../../tsconfig.json",
    "include": ["src/**/*.ts"],
    "exclude": ["node_modules", "**/*.test.*"],
    "compilerOptions": {
        "declaration": true,
        "declarationDir": ".types"
    }
}
